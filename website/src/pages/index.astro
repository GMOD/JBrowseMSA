---
import Viewer from '../components/Viewer.tsx'
import Base from '../layouts/Base.astro'

const base = import.meta.env.BASE_URL
const cards = [
  {
    title: 'User guide',
    body: 'Features, file formats, and a tour of the app.',
    href: `${base}/guide`,
  },
  {
    title: 'Embedding',
    body: 'Add the viewer to your app: props, model API, UMD bundle.',
    href: `${base}/embedding`,
  },
  {
    title: 'Gene explorer',
    body: 'Build a connected JBrowse session from any gene in seven species: collapsed-intron genome view, alignment, and AlphaFold structure.',
    href: `${base}/gene-explorer`,
  },
  {
    title: 'Gallery',
    body: 'Alignments that tell a biological story, plus connected JBrowse sessions.',
    href: `${base}/gallery`,
  },
  {
    title: 'Examples',
    body: 'A live gallery of usage patterns with source.',
    href: `${base}/examples`,
  },
  {
    title: 'Standalone app',
    body: 'Open your own alignment, tree, and domain annotations.',
    href: `${base}/demo/`,
  },
  {
    title: 'R package (msaviewr)',
    body: 'ape, Biostrings, ggtree, treeio, and Shiny interop.',
    href: `${base}/r-package`,
  },
  {
    title: 'CLI',
    body: 'Batch InterProScan → GFF domain annotations.',
    href: `${base}/cli`,
  },
]
---

<Base
  title="JBrowseMSA — multiple sequence alignment viewer"
  description="Interactive multiple sequence alignment viewer with phylogenetic tree, protein domains, and many color schemes. React, HTML, and R."
  wide
>
  <section class="hero">
    <h1>JBrowseMSA</h1>
    <p class="tagline">
      An interactive multiple sequence alignment viewer — phylogenetic tree,
      protein domains, and many color schemes, scaling to very large alignments.
      Everything runs in the browser; nothing is uploaded.
    </p>
  </section>

  <section class="embed">
    <h2>Try it</h2>
    <Viewer client:only="react" base={base} />
  </section>

  <section>
    <h2>Documentation</h2>
    <div class="cards">
      {
        cards.map(card => (
          <a class="card" href={card.href}>
            <h3>{card.title}</h3>
            <p>{card.body}</p>
          </a>
        ))
      }
    </div>
  </section>
</Base>
